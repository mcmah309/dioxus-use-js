// For format details, see https://aka.ms/devcontainer.json. For config options, see the
{
	"name": "dioxus",
	"image": "dioxus",
	"customizations": {
		"vscode": {
			"settings": {
				"extensions.verifySignature": false
			},
			"extensions": [
				// general tools
				"usernamehw.errorlens", // Inline diagnostic errors
				"ryuta46.multi-command", // Used in settings.json to add custom command combinators
				// "streetsidesoftware.code-spell-checker", // Spell check
				"redhat.vscode-yaml", // yaml lsp
                "qufiwefefwoyn.inline-sql-syntax", // sql syntax highlighting in strings with `--sql`
				
				// rust
				"fill-labs.dependi", // dependency validation in Cargo.toml
				"tamasfe.even-better-toml", // toml lsp
				"rust-lang.rust-analyzer", // rust lsp
				"vadimcn.vscode-lldb", // rust debugger
				
				
				"ms-vscode.cpptools", // language support for C/C++, debugging, etc (things like intelliSense/formatting are disabled in favor of clangd)
				"ms-vscode.cmake-tools", // cmake
				"llvm-vs-code-extensions.vscode-clangd", // C/C++ intellisense, formatting, etc
				"vadimcn.vscode-lldb", // C debugger
				
				
				"ziglang.vscode-zig",
				
				
				// dioxus
				"dioxuslabs.dioxus",
				
				
				
				
				// web
				"oven.bun-vscode", // bun
				"ecmel.vscode-html-css", // html/css support
				"bradlc.vscode-tailwindcss", // tailwind css support
				"jock.svg", // svg language support
				"vincaslt.highlight-matching-tag", // highlights matching html tags
				"pranaygp.vscode-css-peek", // Go to definition for css
				"ms-vscode.live-server", // live preview of html
				
				
			]
		}
	},
	"mounts": [
	],
	"runArgs": [
		"--privileged",
		"--net=host",
		"--pids-limit=16384", // Some processes like rustc can spawn more than the default process/thread limit of 2048
		// ssh
		"-v", "/home/henry/.ssh/:/root/.ssh",
		
		
	],
	
	"remoteUser": "root",
}